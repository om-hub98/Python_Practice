Important terminiologies in String :

1) Slicing 
   e.g: name = "Omraj"
   slice "Omr" => name[0,3,1]

   Use case : Find Substring

2) Triming( use strip())
   e.g: name = " Omraj "
   Use Case : remove trailing spaces => name.strip()
